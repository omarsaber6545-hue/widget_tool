# دليل تشغيل ونشر موقع CustomRP 24/7 🚀

تم تصميم **CustomRP Web 24/7** ليعمل بشكل متواصل دون توقف ودون الحاجة لإبقاء جهاز الكمبيوتر الشخصي قيد التشغيل أو فتح تطبيق الديسكورد المكتبي، مع ميزة **استئناف الوقت التراكمي بدقة من اللحظة التي توقف عندها** في حال حدوث أي انقطاع.

---

## 1. التشغيل المحلي السريع (Local Running)

من داخل مجلد `CustomRP-Web`:
```bash
npm install
npm start
```
ثم افتح المتصفح على:
👉 **`http://localhost:3000`**

### تشغيله في الخلفية بدون أن ينغلق (باستخدام PM2):
إذا أردت أن يظل شغالاً في خلفية جهازك حتى لو أغلقت شاشة الأوامر:
```bash
npm install -g pm2
pm2 start src/server.js --name "customrp-247"
pm2 save
pm2 startup
```

---

## 2. التشغيل باستخدام Docker / Docker Compose

إذا كان لديك Docker مثبت:
```bash
docker-compose up -d
```
سيقوم Docker بإعادة تشغيل الحاوية تلقائياً (`restart: always`) إذا حدث أي Crash أو بعد إعادة تشغيل الجهاز.

---

## 3. الاستضافة السحابية المجانية 24/7 (Cloud Hosting)

يمكنك رفع الموقع على أي خدمة سحابية مجانية وسيعمل 24 ساعة في اليوم، مثل:

### أ) النشر على Render.com (مجاني):
1. ارفع مجلد المشروع على حسابك في GitHub.
2. ادخل إلى [render.com](https://render.com) وسجل حساباً.
3. اختر **New +** ثم **Web Service** واربط مستودع GitHub الخاص بك.
4. حدد الإعدادات:
   - **Environment:** `Node`
   - **Root Directory:** `CustomRP-Web`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
5. اضغط **Deploy Web Service**.

### ب) الحفاظ على السيرفر نشطاً 24/7 بدون نوم (Keep-Alive):
الخدمات المجانية مثل Render تدخل في وضع النوم (Sleep) إذا لم يزر الموقع أحد لمدة 15 دقيقة. لحل هذا وجعله يعمل 24 ساعة دون نوم نهائياً:
1. ادخل إلى موقع [UptimeRobot](https://uptimerobot.com) (مجاني).
2. اضغط **Add New Monitor**.
3. اختر **Monitor Type:** `HTTP(s)`.
4. ضع رابط فحص الصحة الخاص بموقعك:
   `https://your-app-name.onrender.com/health`
5. اضبط فترة الفحص على **كل 5 دقائق (Every 5 minutes)**.
6. اضغط **Create Monitor**.
الآن سيقوم UptimeRobot بإرسال تنبيه خفيف كل 5 دقائق، وسيبقى موقعك والديسكورد ريتش بريزنس نشطاً 24/7 للأبد!

---

## 4. كيف تعمل ميزة "استئناف الوقت عند التوقف"؟
- يقوم الخادم كل 5 ثوانٍ بحفظ الثواني المنقضية الفعالة في ملف `data/presence-state.json`.
- عند حدوث أي انقطاع بالإنترنت أو إعادة تشغيل للسيرفر:
  - يتجمد العداد عند آخر ثانية تم تسجيلها.
  - عند إعادة التشغيل، يتم حساب طابع زمني جديد:
    `timestamps.start = Date.now() - savedElapsedSeconds`
  - ديسكورد يحسب الوقت ويظهر في نشاطك مستكملاً من نفس الدقيقة والثانية بالضبط دون أن تعود إلى الصفر!
